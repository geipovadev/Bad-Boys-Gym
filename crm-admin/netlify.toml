[build]
  command = "npm run build"
  publish = "out"
  # Sitio del CRM: cancela el deploy si el commit no tocó crm-admin/.
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- ."

[build.environment]
  NODE_VERSION = "22"
